<div data-role="page">

  <div data-role="header" data-position="inline">
    <%= link_to "Back", :action => :show, :id => @notas.object %>
    <h1>Edit <%= @notas.nota%></h1>
    <%= link_to "Delete", :action => :delete, :id => @notas.object %>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @notas.object %>"/>
      <table>
          
          <tr>
              <td class="itemLabel">Nota: </td>
              <td class="itemValue"><input type="text" name="notas[nota]" value="<%= @notas.nota %>"/></td>
          </tr>
          
      </table>
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
